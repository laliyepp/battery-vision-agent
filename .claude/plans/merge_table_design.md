I want another skill called merge-unified-table, the arg should be a folder, search all files in this folder and its folder that ends with unified_output.json, merge them into a single json with multiple lines, and sort by "签发日期" asc， put the 三电参数_unified_output.json at last line, I think you could write a python tool script to do this and just use the skill to invoke this python tool